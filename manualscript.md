todo script: widget上加一个提示log, 有车辆爆炸，或瞬移暴走，会弹出一个闪红提示
；widget加一个组件，标识车辆爆炸的位置相对于camera屏幕上的方向， 为scoretext block下面加一个得分log组件，分数增减时有提示，提示会在一段时间淡出，（增加描述）；
提出一套合理的分配budget的方案；
增加山体，景观生成；
重构lsystem规则
加入一个widget,设置生成密度，难度，building数量